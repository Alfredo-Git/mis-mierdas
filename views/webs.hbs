<div class="content">
  <div class="row">
    <h1>Urls</h1>
  </div>

  <ul>
    
    {{#each mierdas as |mierda| }}
      {{#if mierda.web}}  
      <li>
        <div class="box">
          <div class="zoom-nail">
            <p>es una URL</p>
          </div>
          <div class="icons">
            <form action="/user/mis-mierdas/{{mierda.id}}/delete" method="post">
              <button type="submit"><i class="fas fa-trash-alt"></i></button>
            </form> 
            <a href="{{mierda.url}}" target="_blank"><i class="fas fa-link"></i></a>
          </div>
        </div>
        <h2>{{mierda.name}}</h2>
      </li>
      {{/if}}
    {{/each}}
    
  </ul>
</div>
