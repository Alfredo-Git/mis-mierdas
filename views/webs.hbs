<div class="content">
  <div class="row">
    <h1>Mis Urls de mierda</h1>
  </div>

  <ul>
    {{#each mierdas as |mierda| }}
    {{#if mierda.urlWeb }}
    <li>
      <div class="box">
        <div class="zoom-nail">
          {{get_video_stream mierda}}
        </div>
        <div class="icons">
          <form action="/urls/{{mierda.id}}/favorite" method="post">
            <button type="submit"><i class="fas fa-star{{#if mierda.favorite}} favorite {{/if}}"></i></button>
          </form> 
          <i class="fas fa-pencil-alt"></i>
          <form action="/urls/{{mierda.id}}/delete" method="post">
            <button type="submit"><i class="fas fa-trash-alt"></i></button>
          </form> 
          <a href="{{mierda.url}}" target="_blank"><i class="fas fa-link"></i></a>
        </div>
      </div>
      <div class="box-info-content">
        <div class="box-info">
          <h2>{{mierda.name}}</h2>
          <form action="/urls/{{mierda.id}}/update" method="post">
            <input type="text" name="name" placeholder="{{mierda.name}}">
            <button class="btn btn-send" type="submit"><i class="fas fa-pencil-alt"></i> update</button>
            <i class="fas fa-times close"></i>
          </form>
        </div>
      </div>
    </li>
  {{/if}}
  {{/each}}
  </ul>
</div>